#!/bin/bash

# Install dependencies
npm install express cors dotenv winston google-ads-api google-auth-library

# Install dev dependencies
npm install --save-dev typescript @types/node @types/express @types/cors @types/winston ts-node nodemon jest ts-jest @types/jest 